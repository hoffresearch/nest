==============================================================================================
TASK #07 :: G1: chunk-to-chunk csr graph_adjacency(0x0C) + search-graph + drop chunk_overlap
phase: phase 1   layer: cross
status:  [ ] todo    [ ] doing    [x] done
==============================================================================================

self-contained checklist. work top to bottom, check each [ ]. read the COMPLETE
files listed before coding. do not summarize, do not skip. add a [ ] if something
is missing. never skip silently.

--- goal ---
ship ratio at near-zero risk on cold metadata sections, land the chunk-to-chunk csr (named
graph-search + reclaims chunk_overlap), and stand up the agent-native flagship (ask + cite +
retrieve()) on phase 0-1. this is the first thing a stranger can pip-install and trust.

--- depends on ---
  previous phase merged

--- READ THESE COMPLETE FILES FIRST ---
[ ] doc/plan/tasks/later/20-csr-graph-adjacency-section-search-graph-subcomm.txt
[ ] doc/plan/tasks/next/10-shared-intpack-codec-delta-zigzag-per-128-block-.txt
[ ] crates/nest-runtime/src/ann/codec.rs
[ ] doc/plan/master-plan/07-graph.txt
[ ] doc/plan/master-plan/03-roadmap.txt
[ ] doc/plan/master-plan/04-risks-quickwins.txt

  (and the current code you will change:)
[ ] crates/nest-format/src/sections/graph/mod.rs
[ ] crates/nest-format/src/sections/graph/adjacency.rs
[ ] crates/nest-runtime/src/graph/csr.rs
[ ] crates/nest-runtime/src/graph/traverse.rs
[ ] crates/nest-cli/src/cmd/search_graph.rs

--- OWNS THESE CODE FILES (no parallel task edits them) ---
  crates/nest-format/src/sections/graph/mod.rs
  crates/nest-format/src/sections/graph/adjacency.rs
  crates/nest-runtime/src/graph/csr.rs
  crates/nest-runtime/src/graph/traverse.rs
  crates/nest-cli/src/cmd/search_graph.rs

--- STEPS (check each) ---
[ ] GRAPH_ADJACENCY_PAYLOAD_VERSION=1 csr (intpack offsets, delta-gapped neighbors, edge-type
    runs) gated behind graph_present capability (delivered via Option<CapabilitiesExt>, not a
    new required bool), excluded from content_hash
[ ] CsrIndex::from_bytes + bounded_bfs off mmap; search_graph seeds exact top-k -> bfs -> the
    refactored score_subset rerank handle, recall NaN; covered by the phase-0 parameterized
    honesty test
[ ] 9th cli subcommand search-graph (kept under the hood); build emits NEXT_CHUNK + top-m
    semantic edges canonically sorted; drop chunk_overlap gated on a recall@10 check

--- draws from ---
graph-db/vesoft-inc__nebula, graph-db/kuzudb__kuzu, graph-db/FalkorDB__FalkorDB,
run-llama/llama_index

--- watch out ---
  the graph frontier must ONLY generate candidates; the phase-0 parameterized test asserts
  search_graph scores byte-equal exact rerank on the union. DECOUPLE from the entity graph in
  the plan text: G1 landing is NOT license to start G2/G3. hard depends-on intpack.

--- NEST RULES (check each) ---
[ ] edition 2024; never panic in lib (typed thiserror). FORGE heavy/non-deterministic work
    stays OUT of the sovereign crates/ workspace.
[ ] format v1 FROZEN + ADDITIVE: no NEST_FORMAT_VERSION bump. use reserved encoding ids 4-9 /
    section ids 0x09+.
[ ] content_hash over DECODED bytes: codecs decode byte-identically so nest:// citations stay
    stable.
[ ] never zstd/dedup/shuffle the embeddings section (mmap SIMD hot path); only fixed-stride
    quantization there.
[ ] deterministic: the .nest build anchor is canonical extracted text + model fingerprint,
    verify byte-identical across two builds.
[ ] <=300 lines per rust src file; docs lowercase, no emoji, no em-dash. feature branch off
    main.

--- DONE WHEN (all green) ---
[ ] cargo build --release --workspace ; cargo test --workspace (+ roundtrip+content_hash test
    for new codecs)
[ ] cargo fmt --all --check ; cargo clippy --workspace --all-targets -- -D warnings
[ ] rebuild python/_nest.so if format/runtime/python changed ; ./scripts/release_check.sh
    passes
[ ] python/tools/measure_presets.py: record size ratio + recall@10 before/after

--- RESULT (filled) ---
  size ratio  before: 0.256  after: 0.256   (tiny preset; graph is additive + default-off, no byte impact)
  recall@10   before: 0.9900 after: 0.9900  (tiny preset, 50 queries vs f32 exact baseline; held)

  delivered (G1, all additive within frozen v1, NO NEST_FORMAT_VERSION bump):
  - SECTION_GRAPH_ADJACENCY (0x0C) chunk-to-chunk csr codec
    (crates/nest-format/src/sections/graph/{mod,adjacency}.rs):
    GRAPH_ADJACENCY_PAYLOAD_VERSION=1; intpack offsets + per-(src,edge_type)
    delta-gapped neighbor ids + falkordb-iso edge-type column inside a RAW
    self-describing section payload (reuses encoding 4 internally, like hnsw
    0x07). edge types NEXT_CHUNK=0 / SEMANTIC=1 / CITATION=2. canonical
    (src,edge_type,dst) sort => byte-identical builds. typed NestError on every
    truncation/hostile claim, never panics (degree + n_nodes-overflow guards).
  - EXCLUDED from content_hash: not in CANONICAL_SECTIONS, joins
    OPTIONAL_SECTIONS so section_name/inspect resolve it. proven by
    graph_adjacency_roundtrip.rs (content_hash equal WITH vs WITHOUT 0x0C) and
    reserved_ids.rs (resolves via section_name yet content_hash-excluded).
  - gated by graph_present via Option<CapabilitiesExt>: the
    NestFileBuilder::graph_adjacency(payload) method sets it; mmap_file opens
    the section only behind that flag (has_graph()).
  - runtime graph::CsrIndex (csr.rs, neighbors(node) = contiguous slice off the
    parsed columns) + bounded_bfs (traverse.rs, generational visited buffer,
    no per-query HashSet, hops + max_frontier bounds, dedup, seeds-first).
  - search_graph(query,k,hops,ef): exact-cosine top-ef seed -> bounded_bfs ->
    union seed+frontier -> score_subset exact rerank -> top-k.
    index_type="graph", recall=NaN. falls back to search() when no 0x0C section.
  - 9th cli subcommand `nest search-graph <file> <q-json> -k --hops --ef` +
    PyO3 NestFile.search_graph + has_graph.
  - python build path: nest.build(..., with_graph=True, graph_top_m=8) emits
    NEXT_CHUNK (sequential, both dirs) + top-m SEMANTIC edges (from the already-
    built hnsw level-0 graph, no o(n^2) knn), canonically sorted; default off.
    chunk_overlap drop is opt-in (BuildConfig.drop_overlap implies with_graph;
    read-side context via python/graph_context.neighbor_context) GATED on
    python/tools/graph_recall_gate.py (recall@10 vs baseline; PASS at 1.0000).
  - HONESTY GATE: rerank_contract.rs adds 'graph' to non_exact_paths; the
    fixture carries a 0x0C section and asserts search_graph scores are
    byte-equal to the exact-cosine rerank on the union and recall=NaN.

  gate: cargo build --release --workspace OK; cargo fmt --all --check OK;
        cargo clippy --workspace --all-targets -D warnings OK; cargo test
        --release --workspace OK (0 failures); measure tiny 0.256 / recall@10
        0.9900 held; graph_recall_gate.py PASS (recall@10 1.0000).

--- DOC CHECKPOINT (virtuous cycle: keep the living docs in sync, as a team) ---
update only what THIS task actually changed; check each:
[ ] doc/arc/arc.md     architecture inventory + contract (boundaries/sections/data-flow/runtime)
[ ] doc/arc/arc.yaml   machine architecture map (MUST move with arc.md, same commit)
[ ] doc/arc/arc.mmd    mermaid build/query flow (if the flow changed)
[ ] README.md          overview / cli / presets / highlights (if a user-visible surface changed)
[ ] doc/usage.md       the how-to (if a command, flag, preset, or workflow changed)
[ ] AGENTS.md          build/test cmds, conventions, gotchas, known gaps (if any changed)
[ ] Cargo.toml         workspace version / deps / new crate (if crates or deps changed)
[ ] pyproject.toml     python tooling / deps / targets (if the python surface changed)
[ ] rustfmt.toml       only if a formatting rule must change (rare)
the arc trio (arc.md + arc.yaml + arc.mmd) moves TOGETHER in the same commit when
architecture / boundaries / data-flow / contracts / storage / runtime change.

--- MERGE ---
[ ] commit on branch feat/p1-07-g1-chunk-to-chunk-csr-graph-adjacency-0x0c-sea
[ ] gate green -> merge, mark done in plan/kanban.txt
