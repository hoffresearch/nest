==============================================================================================
TASK #11 :: trained dict + fsst + block/rotational int8 + micro preset
phase: phase 1 (PRE-GATE, pulled forward in the compression rebalance)   layer: nest
status:  [ ] todo    [ ] doing    [ ] done
==============================================================================================

NOTE (from #06 diligence, 2026-06-06): the dict/fsst TEXT ratio gain needs the canonical text
section split into per-chunk independent streams (the O(1) single-chunk reopen layout); today it
is ONE concatenated zstd-19 blob where a trained dict and fsst do not beat zstd, so that layout
change is a prerequisite and is out of this task's current scope.

self-contained checklist. work top to bottom, check each [ ]. read the COMPLETE
files listed before coding. do not summarize, do not skip. add a [ ] if something
is missing. never skip silently.

--- goal ---
land the two SAFE headline ratio levers PRE-GATE so the flagship is born compressing: trained
zstd dict + fsst on text, and block/rotational int8 at ~4x near-lossless. ship a micro preset
below tiny 0.283 with measured recall - this is the number the gate publishes. the risky
sub-byte step (int4/rabitq) stays post-gate.

--- depends on ---
  phase-0 codec registry (#02) + honest-rerank handle (#01) + roundtrip/recall harness (#03),
  all merged. PRE-GATE: this IS part of the flagship proof, not gated behind it.

--- READ THESE COMPLETE FILES FIRST ---
[ ] doc/plan/master-plan/01-compression.txt
[ ] doc/research/genome/compression-general/facebook__zstd.txt
[ ] doc/research/synthesis/vector-quantization.txt
[ ] doc/plan/master-plan/02-format.txt
[ ] doc/plan/master-plan/03-roadmap.txt
[ ] doc/plan/master-plan/04-risks-quickwins.txt

  (and the current code you will change:)
[ ] crates/nest-format/src/encoding/zstd_dict.rs
[ ] crates/nest-format/src/encoding/fsst.rs
[ ] crates/nest-format/src/encoding/int8.rs
[ ] crates/nest-format/src/encoding/fwht.rs

--- OWNS THESE CODE FILES (no parallel task edits them) ---
  crates/nest-format/src/encoding/zstd_dict.rs
  crates/nest-format/src/encoding/fsst.rs
  crates/nest-format/src/encoding/int8.rs
  crates/nest-format/src/encoding/fwht.rs

--- STEPS (check each) ---
[ ] trained shared zstd dictionary(0x0A) + fsst(9) + zstd level/ldm knobs (sorted samples,
    pinned zstd version for reproducibility)
[ ] int8 scale_kind=1 block-scaled + seeded-FWHT rotational int8 (~0.99 recall at 4x, no new
    id), rotation computed backend-independently at build time for cross-cpu determinism
[ ] matryoshka prefix truncation + micro preset below tiny 0.283 with measured recall, ratio
    reported net of fp where sub-int8

--- draws from ---
facebook/zstd, ggml-org/ggml, weaviate/recipes, QwenLM/Qwen3-Embedding

--- watch out ---
  ZDICT must be fed sorted samples and pinned to a zstd version or reproducible builds break;
  fwht rotation determinism hinges on identical float ops - compute backend-independently at
  build time.

--- NEST RULES (check each) ---
[ ] edition 2024; never panic in lib (typed thiserror). FORGE heavy/non-deterministic work
    stays OUT of the sovereign crates/ workspace.
[ ] format v1 FROZEN + ADDITIVE: no NEST_FORMAT_VERSION bump. use reserved encoding ids 4-9 /
    section ids 0x09+.
[ ] content_hash over DECODED bytes: codecs decode byte-identically so nest:// citations stay
    stable.
[ ] never zstd/dedup/shuffle the embeddings section (mmap SIMD hot path); only fixed-stride
    quantization there.
[ ] deterministic: the .nest build anchor is canonical extracted text + model fingerprint,
    verify byte-identical across two builds.
[ ] <=300 lines per rust src file; docs lowercase, no emoji, no em-dash. feature branch off
    main.

--- DONE WHEN (all green) ---
[ ] cargo build --release --workspace ; cargo test --workspace (+ roundtrip+content_hash test
    for new codecs)
[ ] cargo fmt --all --check ; cargo clippy --workspace --all-targets -- -D warnings
[ ] rebuild python/_nest.so if format/runtime/python changed ; ./scripts/release_check.sh
    passes
[ ] python/tools/measure_presets.py: record size ratio + recall@10 before/after

--- RESULT (fill in) ---
  size ratio  before: ____  after: ____
  recall@10   before: ____  after: ____

--- WO EMB-matryoshka RESULT (2026-06-06, branch research/nest-genome-v2) ---
  step 3 of this task (matryoshka prefix truncation + micro preset) shipped as a
  standalone WO. build-time truncate-then-renormalize (Qwen3/ST/BGE): nest.build
  gains an mrl_dim kwarg; each l2-normalized f32 row is sliced to the prefix and
  re-l2-normalized BEFORE quantization, so int8/int4 calibrate on the shorter
  renormalized row. header/manifest embedding_dim becomes mrl_dim; full_dim +
  mrl_dim recorded as additive optional manifest fields (unset files stay
  byte-identical). NO runtime kernel change (reader strides by header.embedding_dim).
  determinism verified: two builds at the same mrl_dim are byte-identical.

  measured on dat/corpus_next.v1.nest (n=30725, dim=384 MiniLM, NOT mrl-trained),
  100 queries, k=10, recall vs the full-dim float32 baseline:

    f32 truncation-only curve (isolates the dimension axis):
      mrl256 ratio 0.419 recall 0.811 | mrl192 0.353 0.732 | mrl128 0.288 0.657 | mrl96 0.255 0.574
    int8 ladder {256,192,128,96}:
      mrl256-int8 0.223 0.810 | mrl192 0.207 0.733 | mrl128 0.190 0.659 | mrl96 0.182 0.574
    int4 ladder {256,192,128} (96 BLOCKED: 96%64=32, int4 needs dim%64==0):
      mrl256-int4 0.191 0.777 | mrl192 0.183 0.713 | mrl128 0.174 0.627

  honest finding: MiniLM front-loading is NOT guaranteed, so truncation costs
  real recall (int8/f32 curves nearly identical => the loss is the dimension axis,
  quant adds ~nothing at int8, ~3-5 pts at int4). NO ladder point holds the nano
  floor (recall@10>=0.85): the existing nano (int4 full-dim, 0.209, 0.913) still
  WINS on recall while every mrl point is smaller. the smallest point within a
  reported tolerance is mrl256-int8 (0.223 ratio, 0.810 recall): the micro lever
  to use ONLY when size beats the last ~10 recall points, or once a real
  mrl-trained model lands (the curve front-loads and these numbers jump). reported
  as a curve, not a cherry-pick; size lever disclosed with measured recall@10 +
  stated dim/dtype per COMPRESSION-HONESTY. conditional micro-mrl gate added to
  compare_measure.py (mrl256-int8: size_ratio<=0.25, recall@k>=0.78). all green:
  build+fmt+clippy+test pass, measure shows recall held.
  (the dict/fsst/rotational-int8 steps of #11 remain todo, out of this WO's scope.)

--- WO TXT-streams RESULT (2026-06-06, branch research/nest-genome-v2) ---
  the NOTE prerequisite at the top of this task (canonical text split into
  per-chunk independent streams = the O(1) single-chunk reopen layout)
  shipped as a standalone WO. new wire encoding id 10 = txt_streams: the
  chunks_canonical (0x02) COMPRESSED form is re-laid-out from one
  concatenated zstd-19 blob into N independently zstd-encoded streams (one
  per canonical string) behind an intpack (id 4, reused) offset table of
  n+1 byte offsets giving O(1) seek to any chunk. decode() rebuilds the
  EXACT canonical payload (12-byte prefix + per chunk u32 len + utf-8)
  BYTE-IDENTICALLY to sections::encode_chunks_canonical, so content_hash and
  nest:// citations are unchanged (validated against the intpack-repack
  precedent). additive within frozen v1: NO NEST_FORMAT_VERSION bump; ids
  5(zstd_dict) and 9(fsst) kept free for the codecs this unlocks.

  wired into the builder under a compressed (text_encoding=Zstd) preset
  ONLY: chunks_canonical takes the SMALLER of single-frame maybe_zstd vs the
  per-chunk txt-streams form (same try-smaller as spans intpack-vs-zstd), so
  the build never regresses. raw-text presets and the golden fixture
  (golden_v1_minimal.nest, 1366 bytes) stay byte-identical (golden uses raw
  text, untouched). reader/validate.rs extends the non-embedding allow-list
  to raw/zstd/intpack/txt_streams.

  HONEST MEASUREMENT (dat/corpus_next.v1.nest, n=30725, dim=384 MiniLM):
    chunks_canonical text section, single-frame zstd-19 vs per-chunk
    txt_streams (measured via the real codec on the corpus text):
      single-frame zstd-19 = 15,730,724 B   (the existing form)
      per-chunk txt_streams = 29,247,724 B   (+85.9%, +13.5MB)
    => the chooser keeps single-frame zstd; the compressed/tiny/nano files
       are BYTE-IDENTICAL before/after (chunks_canonical encoding stays
       zstd=1, size 15,730,724 B confirmed in the built corpora). overall
       size_ratio UNCHANGED (tiny 0.256, recall@10 0.9900 held vs baseline).
    the +85.9% is the expected, disclosed cross-chunk-context loss: a per-
    chunk frame is where a trained dict/fsst can later beat one big zstd-19
    blob (and the O(1) reopen layout), so this WO ships the LAYOUT that
    unlocks the ratio win, not the ratio win itself. the runtime O(1)
    single-chunk consumer (cite/materialize through a per-chunk accessor)
    is the follow-on, kept OUT of scope to keep the diff additive (the
    TxtStreams reader's O(1) text(i) accessor is the on-disk handle).
  all green: build+fmt+clippy+test pass, measure shows recall held.

--- DOC CHECKPOINT (virtuous cycle: keep the living docs in sync, as a team) ---
update only what THIS task actually changed; check each:
[ ] doc/arc/arc.md     architecture inventory + contract (boundaries/sections/data-flow/runtime)
[ ] doc/arc/arc.yaml   machine architecture map (MUST move with arc.md, same commit)
[ ] doc/arc/arc.mmd    mermaid build/query flow (if the flow changed)
[ ] README.md          overview / cli / presets / highlights (if a user-visible surface changed)
[ ] doc/usage.md       the how-to (if a command, flag, preset, or workflow changed)
[ ] AGENTS.md          build/test cmds, conventions, gotchas, known gaps (if any changed)
[ ] Cargo.toml         workspace version / deps / new crate (if crates or deps changed)
[ ] pyproject.toml     python tooling / deps / targets (if the python surface changed)
[ ] rustfmt.toml       only if a formatting rule must change (rare)
the arc trio (arc.md + arc.yaml + arc.mmd) moves TOGETHER in the same commit when
architecture / boundaries / data-flow / contracts / storage / runtime change.

--- MERGE ---
[ ] commit on branch feat/p1-11-trained-dict-fsst-block-rotational-int8-micro-
[ ] gate green -> merge, mark done in plan/kanban.txt
