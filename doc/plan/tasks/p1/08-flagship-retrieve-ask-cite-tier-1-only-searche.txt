==============================================================================================
TASK #08 :: flagship retrieve() + ask + cite (tier-1 only) + SearchExplain
phase: phase 1   layer: lens
status:  [ ] todo    [ ] doing    [ ] done
==============================================================================================

self-contained checklist. work top to bottom, check each [ ]. read the COMPLETE
files listed before coding. do not summarize, do not skip. add a [ ] if something
is missing. never skip silently.

--- goal ---
ship ratio at near-zero risk on cold metadata sections, land the chunk-to-chunk csr (named
graph-search + reclaims chunk_overlap), and stand up the agent-native flagship (ask + cite +
retrieve()) on phase 0-1. this is the first thing a stranger can pip-install and trust.

--- depends on ---
  previous phase merged

--- READ THESE COMPLETE FILES FIRST ---
[ ] doc/research/synthesis/bindings-cli.txt
[ ] doc/research/synthesis/rag-orchestration.txt
[ ] crates/nest-cli/src/cmd/cite.rs
[ ] crates/nest-runtime/src/search.rs
[ ] doc/plan/master-plan/09-lens.txt
[ ] doc/plan/master-plan/03-roadmap.txt
[ ] doc/plan/master-plan/04-risks-quickwins.txt

  (and the current code you will change:)
[ ] crates/nest-cli/src/cmd/ask.rs
[ ] crates/nest-cli/src/cmd/retrieve.rs
[ ] crates/nest-python/src/retrieve_fn.rs
[ ] python/forge/retrieve.py

--- OWNS THESE CODE FILES (no parallel task edits them) ---
  crates/nest-cli/src/cmd/ask.rs
  crates/nest-cli/src/cmd/retrieve.rs
  crates/nest-python/src/retrieve_fn.rs
  python/forge/retrieve.py

--- STEPS (check each) ---
[ ] ship TWO user-facing verbs ask + cite over the existing engine; keep the existing 8
    subcommands as-is under the hood (DEFER verb-collapse, the nest dev namespace, and the
    nest-profile crate - they are churn with no user value pre-users)
[ ] add the additive SearchExplain field on SearchResult; --disclose explain on ask prints the
    rerank-source honesty line (real cosine vs real cosine at stored precision)
[ ] retrieve() verb + python retrieve(): agent-shaped cited spans where the returned score IS
    the exact-cosine rerank value; cite is TIER-1 ONLY (returns stored canonical text +
    verifying hashes, exactly what cite.rs does today); the flagship work-order MUST NOT claim
    original-byte reopen; one-gif demo on the phase-0 demo corpus

--- draws from ---
clap-rs/clap, langchain-ai/langchain, supermemoryai/supermemory, PyO3/pyo3

--- watch out ---
  the flagship is a lie if retrieve ever returns a candidate-generator score instead of the
  exact rerank value (the phase-0 test guards this) OR if cite claims to reopen original bytes
  (it returns stored canonical text today; original-byte reopen is net-new and belongs to
  catalog, not the flagship).

--- NEST RULES (check each) ---
[ ] edition 2024; never panic in lib (typed thiserror). FORGE heavy/non-deterministic work
    stays OUT of the sovereign crates/ workspace.
[ ] format v1 FROZEN + ADDITIVE: no NEST_FORMAT_VERSION bump. use reserved encoding ids 4-9 /
    section ids 0x09+.
[ ] content_hash over DECODED bytes: codecs decode byte-identically so nest:// citations stay
    stable.
[ ] never zstd/dedup/shuffle the embeddings section (mmap SIMD hot path); only fixed-stride
    quantization there.
[ ] deterministic: the .nest build anchor is canonical extracted text + model fingerprint,
    verify byte-identical across two builds.
[ ] <=300 lines per rust src file; docs lowercase, no emoji, no em-dash. feature branch off
    main.

--- DONE WHEN (all green) ---
[ ] cargo build --release --workspace ; cargo test --workspace (+ roundtrip+content_hash test
    for new codecs)
[ ] cargo fmt --all --check ; cargo clippy --workspace --all-targets -- -D warnings
[ ] rebuild python/_nest.so if format/runtime/python changed ; ./scripts/release_check.sh
    passes
[ ] python/tools/measure_presets.py: record size ratio + recall@10 before/after

--- RESULT (fill in) ---
  size ratio  before: 0.256  after: 0.256   (tiny preset; this WO is read-side only, zero format bytes)
  recall@10   before: 0.9900 after: 0.9900  (tiny, NEST_QUERIES=30, vs the f32 exact baseline; held)

  delivered (additive, read-side only; no NEST_FORMAT_VERSION bump, no format bytes):
  - additive SearchExplain { route, exact/ann/bm25/graph_candidates, fusion_mode,
    rerank_source: RerankSourceKind, recall_estimate } on SearchResult (nest-runtime/src/lib.rs),
    populated on all four search fns (nest-runtime/src/search.rs). rerank_source is the honesty
    backbone: FullPrecision="real cosine" (0x09 fp slab present or stored f32) vs
    StoredPrecision="real cosine at stored precision" (lossy stored slab, no fp).
  - nest ask (cmd/ask.rs): offline potion embed + model_hash gate + capability route, --disclose
    answer (cited text + nest:// only) | explain (adds the rerank-source honesty line + route).
  - nest retrieve (cmd/retrieve.rs): json/jsonl answer-pack where score IS the exact rerank value,
    tier-1 stored canonical text + hashes + citation_id; cite round-trips.
  - NestFile.retrieve (nest-python/src/retrieve_fn.rs, RetrieveHitPy) + re-exported on python/nest.py;
    python/forge/retrieve.py (offline potion-embed -> retrieve + one-gif demo over demo_corpus);
    python/forge/embed_query_potion.py (the offline embedder the cli verbs shell out to).
  - cite stays TIER-1 ONLY (stored canonical text + verifying hashes); no original-byte reopen claim.
  - DEFERRED per WO + risks-quickwins: verb-collapse, nest dev namespace, nest-profile crate,
    --disclose hits/trace, graph/export/cosmos verbs, tier-2 original-byte reopen, maturin/PyPI,
    zero-copy PyBuffer, weighted/relativeScore fusion.

  research_drawn (concrete techniques used, recorded per the WO):
  - lens (doc/plan/master-plan/09-lens.txt): the exact SearchExplain field shape, the
    --disclose answer|explain levels, the rerank-source honesty line wording, and the
    capability-route default (exact if only embeddings, hybrid if bm25, +graph if adjacency).
  - risks-quickwins (04): "ship ask+cite+retrieve as soon as phase 0-1 is green; needs no new
    format and no original-byte reopen"; "add SearchExplain during phase-0 so the disclose line
    and retrieve provenance come free"; the tier-1/tier-2 cite split (cite returns stored canonical
    text today, original-byte reopen is net-new tier-2); the UX-bloat mitigation (ship exactly
    ask+cite, keep the 8 subcommands under the hood, defer verb-collapse/nest-profile);
    the default static potion embedder is the offline flagship blocker.
  - rag-orchestration synthesis + langchain ref: content_hash citation_id as the record key;
    "embeddings stored quantized but always paired with an exact rescoring pass" (= score IS the
    exact rerank value, the flagship-is-a-lie guard); model2vec/POTION offline static embedder so
    the embed step itself is sovereign (no sentence-transformers on the ask path).
  - bindings-cli synthesis + pyo3 ref: keep PyO3 confined to nest-python, typed thiserror ->
    PyValueError never a panic (retrieve_fn.rs), offline-by-construction no-socket (the watch-out
    that the ask path must NEVER spawn the sentence-transformers embedder).
  - supermemory ref: the agent-native answer-pack is the smallest most-defensible surface
    (cited offline retrieval), reinforcing the flagship scoping.
  - clap ref: thin clap verb surface; did_you_mean/dynamic-completion noted as out of scope here.

--- DOC CHECKPOINT (virtuous cycle: keep the living docs in sync, as a team) ---
update only what THIS task actually changed; check each:
[ ] doc/arc/arc.md     architecture inventory + contract (boundaries/sections/data-flow/runtime)
[ ] doc/arc/arc.yaml   machine architecture map (MUST move with arc.md, same commit)
[ ] doc/arc/arc.mmd    mermaid build/query flow (if the flow changed)
[ ] README.md          overview / cli / presets / highlights (if a user-visible surface changed)
[ ] doc/usage.md       the how-to (if a command, flag, preset, or workflow changed)
[ ] AGENTS.md          build/test cmds, conventions, gotchas, known gaps (if any changed)
[ ] Cargo.toml         workspace version / deps / new crate (if crates or deps changed)
[ ] pyproject.toml     python tooling / deps / targets (if the python surface changed)
[ ] rustfmt.toml       only if a formatting rule must change (rare)
the arc trio (arc.md + arc.yaml + arc.mmd) moves TOGETHER in the same commit when
architecture / boundaries / data-flow / contracts / storage / runtime change.

--- MERGE ---
[ ] commit on branch feat/p1-08-flagship-retrieve-ask-cite-tier-1-only-searche
[ ] gate green -> merge, mark done in plan/kanban.txt
