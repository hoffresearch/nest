============================================================================================
TASK #16 :: int4 block-64 codec + fused dequant+dot kernel
column: later    priority: MEDIUM
status:  [ ] todo    [ ] doing    [ ] done
============================================================================================

this file is the ONLY thing you need for this task. work top to bottom and
check each [ ] as you finish it. if something is missing, ADD a [ ] line and
note it. never skip a box silently. that is how we avoid drift.

--- goal ---
SECTION_ENCODING_INT4=7 (~4.25bpw), split pack/unpack from kernel, fused SQNBitGemm-style
kernel that never materializes f32 in RAM, DType::Int4 arm. Gate the preset on the
embeddings_fp rerank source being present. Roughly halves embeddings vs int8.

--- depends on (must be done + merged first) ---
  task #06, task #07, task #08, task #09

--- READ THESE COMPLETE FILES FIRST (do not summarize, do not skip) ---
[ ] doc/research/genome/model-runtime/microsoft__onnxruntime.txt
[ ] doc/research/synthesis/model-runtime.txt
[ ] doc/plan/master-plan/01-compression.txt
[ ] doc/plan/master-plan/02-format.txt
[ ] doc/plan/master-plan/04-risks-quickwins.txt

  (and read the current code you will change:)
[ ] crates/nest-format/src/encoding/int4.rs
[ ] crates/nest-runtime/src/simd/int4.rs
[ ] crates/nest-runtime/src/search.rs

--- OWNS THESE CODE FILES (no other parallel task may edit them) ---
  crates/nest-format/src/encoding/int4.rs
  crates/nest-runtime/src/simd/int4.rs
  crates/nest-runtime/src/search.rs

--- STEPS (check each) ---
[ ] Add SECTION_ENCODING_INT4=7 block-64 codec (~4.25bpw) with split pack/unpack and a fused
    dequant+dot kernel (SQNBitGemm pattern) that never materializes f32 in RAM; DType::Int4
    arm in score_all/score_subset
[ ] Deterministic RTN max-abs-per-block (no calibration corpus); dim%64 fallback to block-32
    int8
[ ] Optional build-time PCA-whitening before quantize for free quality (rotation stored in
    provenance)

--- watch out ---
  Without the Phase-0 fp rerank source int4 makes the contract a half-truth; gate the int4
  preset on embeddings_fp being present, and verify it scores packed blocks (no f32 RAM
  blow-up).

--- NEST RULES (do not violate; check each) ---
[ ] edition 2024, resolver 3. never panic in lib code: typed thiserror, incl. on malformed
    mmap bytes.
[ ] format v1 is FROZEN + ADDITIVE: do NOT bump NEST_FORMAT_VERSION. use reserved encoding
    ids 4-9 / section ids 0x09-0x10 (already named in layout/mod.rs).
[ ] content_hash is over DECODED bytes: any new codec must decode byte-identically so
    nest:// citations stay stable.
[ ] never zstd/dedup/shuffle the embeddings section (scored from mmap on the SIMD hot path);
    only fixed-stride quantization codecs there.
[ ] deterministic: same input + seed => byte-identical output; verify across two builds (and
    SIMD backends for rotations).
[ ] <=300 lines per rust file in crates/**/src/**; split pack/unpack/kernel/select.
[ ] docs lowercase, no emoji, no em-dash. work on a feature branch off main.

--- DONE WHEN (all green) ---
[ ] cargo build --release --workspace
[ ] cargo test --workspace  (add a roundtrip + content_hash-equality test for any new codec)
[ ] cargo fmt --all --check  &&  cargo clippy --workspace --all-targets -- -D warnings
[ ] rebuild python/_nest.so if you touched nest-format / nest-runtime / nest-python
[ ] ./scripts/release_check.sh passes (the project gate; do not bypass)
[ ] python/tools/measure_presets.py: record size ratio + recall@10 before/after

--- RESULT (fill in) ---
  size ratio  before: ______   after: ______
  recall@10   before: ______   after: ______

--- MERGE ---
[ ] commit on branch feat/later-16-int4-block-64-codec-fused-dequant-dot-kernel
[ ] gate green -> merge, then mark this task done in plan/kanban.txt
