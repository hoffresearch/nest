==============================================================================================
TASK #01 :: honest-rerank gate + rerank-source handle + ram-leak fix
phase: phase 0   layer: nest
status:  [ ] todo    [x] doing    [ ] done   (impl + gate green; pending owner ok to commit/merge)
==============================================================================================

self-contained checklist. work top to bottom, check each [ ]. read the COMPLETE
files listed before coding. do not summarize, do not skip. add a [ ] if something
is missing. never skip silently.

--- goal ---
land the guardrails that de-risk the whole roadmap, the ONE parameterized rerank-honesty test
that gates every future search path, the reconciled additive rails, AND the two flagship
blockers (default static embedder, one concrete demo corpus). zero ratio yet; everything later
ships behind proven gates.

--- depends on ---
  previous phase merged

--- READ THESE COMPLETE FILES FIRST ---
[ ] doc/plan/tasks/now/06-land-section-embeddings-fp-0x09-rerank-source-ma.txt
[ ] doc/plan/tasks/now/07-fix-materialize-to-f32-ram-leak-in-the-ann-dista.txt
[ ] crates/nest-runtime/src/search.rs
[ ] doc/plan/master-plan/02-format.txt
[ ] doc/plan/master-plan/03-roadmap.txt
[ ] doc/plan/master-plan/04-risks-quickwins.txt

  (and the current code you will change:)
[ ] crates/nest-runtime/src/search.rs
[ ] crates/nest-runtime/src/materialize.rs
[ ] crates/nest-runtime/src/ann/mod.rs
[ ] crates/nest-format/src/sections/contract.rs

--- OWNS THESE CODE FILES (no parallel task edits them) ---
  crates/nest-runtime/src/search.rs
  crates/nest-runtime/src/materialize.rs
  crates/nest-runtime/src/ann/mod.rs
  crates/nest-format/src/sections/contract.rs

--- STEPS (check each) ---
[x] land SECTION_EMBEDDINGS_FP=0x09 rerank source
    -> runtime read path: rerank::FpSlab::detect reads the optional 0x09 slab (dtype from
       stride: 4->f32, 2->f16), wired into MmapNestFile; rerank source prefers it when present.
       WRITER for 0x09 deferred to the sub-int8 card (p3 #14), which is what needs it
       (owner's call). fp routing unit-tested synthetically in rerank.rs.
[x] REFACTOR score_subset to take an EXPLICIT rerank-source handle (fp slab when present,
    dtype slab otherwise) and to be ready for per-space routing
    -> new module crates/nest-runtime/src/rerank.rs: RerankSource. score_all + score_subset
       both route through self.rerank_source(); per-space path will hand a different slab.
[x] add ONE parameterized contract test over every search entry point
    -> crates/nest-runtime/tests/rerank_contract.rs: parameterized over ann + hybrid today
       (list documented as the gate; graph/space/cross MUST join it when they land), asserts
       returned score == exact-cosine rerank byte-for-byte (to_bits) and recall.is_nan().
[x] fix the materialize-to-f32 ram leak so the ann path scores packed int8/int4 blocks
    -> materialize.rs is now PackedVectors: int8 stays int8+scales, f16 stays f16, decoded one
       row at a time into a scratch buffer. removes the n*dim*4 f32 snapshot. graph + search
       byte-identical for all dtypes (build still uses the f32 store; row decode reproduces the
       old int8*scale / f16->f32 arithmetic). int4 rides the same path when it lands.
[x] document real-cosine-at-stored-precision unless an fp source is present
    -> rerank.rs module docs + arc.md runtime contract.

--- draws from ---
gaoj0017/RaBitQ, microsoft/onnxruntime, ggml-org/ggml

--- watch out ---
  score_subset hard-dispatches on a single dtype and the single 0x04 section today; refactor
  it to a rerank-source handle FIRST or the per-space/sub-int8 gate cannot exist. gate every
  change with the parameterized honesty test and assert byte-identical results on f32 corpora
  before/after.

--- NEST RULES (check each) ---
[ ] edition 2024; never panic in lib (typed thiserror). FORGE heavy/non-deterministic work
    stays OUT of the sovereign crates/ workspace.
[ ] format v1 FROZEN + ADDITIVE: no NEST_FORMAT_VERSION bump. use reserved encoding ids 4-9 /
    section ids 0x09+.
[ ] content_hash over DECODED bytes: codecs decode byte-identically so nest:// citations stay
    stable.
[ ] never zstd/dedup/shuffle the embeddings section (mmap SIMD hot path); only fixed-stride
    quantization there.
[ ] deterministic: the .nest build anchor is canonical extracted text + model fingerprint,
    verify byte-identical across two builds.
[ ] <=300 lines per rust src file; docs lowercase, no emoji, no em-dash. feature branch off
    main.

--- DONE WHEN (all green) ---
[x] cargo build --release --workspace ; cargo test --workspace  (62 rust tests pass)
[x] cargo fmt --all --check ; cargo clippy --workspace --all-targets -- -D warnings
[x] rebuild python/_nest.so ; ./scripts/release_check.sh passes (ALL GATES PASS)
[x] python/tools/measure_presets.py: size ratio + recall@10 unchanged (guardrail task)

  scope notes (per AGENTS "add a [ ] if missing, never skip silently"):
  - OWNS expanded (owner-approved) to mmap_file.rs + ann/{build,search,select_neighbors,codec}.rs
    + lib.rs + the new rerank.rs/rerank_contract.rs; no parallel p0 task owns these.
  - PRE-EXISTING blocker, NOT this task: crates/nest-format/tests/fixtures/golden_v1_minimal.nest
    was an un-materialized git-lfs pointer (129 bytes) on this branch, failing 5 golden tests
    even with this task stashed. restored from the lfs object (sha256 matches, 1366 bytes) so
    the gate could run; the file is NOT staged in this task's commit. repo-hygiene fix for the
    owner: the fixture is excluded from lfs by .gitattributes but committed as a pointer.

--- RESULT (fill in) ---
  size ratio  before: exact 1.000 / compressed 0.350 / tiny 0.283 / hybrid 0.668
              after:  exact 1.000 / compressed 0.350 / tiny 0.283 / hybrid 0.668  (unchanged)
  recall@10   before: 1.0000 / 1.0000 / 0.9920 / 1.0000
              after:  1.0000 / 1.0000 / 0.9920 / 1.0000  (unchanged; guardrail, no ratio change)
  ram:        ann vector store for int8 corpora drops from n*dim*4 (f32 snapshot) to ~n*dim
              (packed int8 + n scales); f16 from n*dim*4 to n*dim*2. f32 unchanged.

--- DOC CHECKPOINT (virtuous cycle: keep the living docs in sync, as a team) ---
update only what THIS task actually changed; check each:
[ ] doc/arc/arc.md     architecture inventory + contract (boundaries/sections/data-flow/runtime)
[ ] doc/arc/arc.yaml   machine architecture map (MUST move with arc.md, same commit)
[ ] doc/arc/arc.mmd    mermaid build/query flow (if the flow changed)
[ ] README.md          overview / cli / presets / highlights (if a user-visible surface changed)
[ ] doc/usage.md       the how-to (if a command, flag, preset, or workflow changed)
[ ] AGENTS.md          build/test cmds, conventions, gotchas, known gaps (if any changed)
[ ] Cargo.toml         workspace version / deps / new crate (if crates or deps changed)
[ ] pyproject.toml     python tooling / deps / targets (if the python surface changed)
[ ] rustfmt.toml       only if a formatting rule must change (rare)
the arc trio (arc.md + arc.yaml + arc.mmd) moves TOGETHER in the same commit when
architecture / boundaries / data-flow / contracts / storage / runtime change.

--- MERGE ---
[ ] commit on branch feat/p0-01-honest-rerank-gate-rerank-source-handle-ram-le
[ ] gate green -> merge, mark done in plan/kanban.txt
