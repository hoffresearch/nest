==============================================================================================
TASK #05 :: forge-0: .fci freeze + books profile (parallel, touches zero core files, ONE chunker)
phase: phase 0   layer: forge
status:  [ ] todo    [ ] doing    [x] done   (FORGE-0a (forge-core workspace + .fci freeze) merged; normalize/pdf/adapter are forge-0b (#09))
         gate green; normalize/pdf/adapter/cli are forge-0b/#09, deferred per the owner)
==============================================================================================

self-contained checklist. work top to bottom, check each [ ]. read the COMPLETE
files listed before coding. do not summarize, do not skip. add a [ ] if something
is missing. never skip silently.

--- goal ---
land the guardrails that de-risk the whole roadmap, the ONE parameterized rerank-honesty test
that gates every future search path, the reconciled additive rails, AND the two flagship
blockers (default static embedder, one concrete demo corpus). zero ratio yet; everything later
ships behind proven gates.

--- depends on ---
  previous phase merged

--- READ THESE COMPLETE FILES FIRST ---
[ ] python/builder.py
[ ] python/model_fingerprint.py
[ ] doc/plan/master-plan/00-vision.txt
[ ] doc/research/synthesis/data-pipeline.txt
[ ] doc/plan/master-plan/05-forge.txt
[ ] doc/plan/master-plan/03-roadmap.txt
[ ] doc/plan/master-plan/04-risks-quickwins.txt

  (and the current code you will change:)
[ ] forge-core/Cargo.toml
[ ] forge-core/src/fci/
[ ] forge-core/src/normalize/
[ ] forge-core/src/extract/pdf_text.rs
[ ] forge-core/src/profile/
[ ] python/forge/adapter.py
[ ] python/forge/cli.py

--- OWNS THESE CODE FILES (no parallel task edits them) ---
  forge-core/Cargo.toml
  forge-core/src/fci/
  forge-core/src/normalize/
  forge-core/src/extract/pdf_text.rs
  forge-core/src/profile/
  python/forge/adapter.py
  python/forge/cli.py

--- STEPS (check each) ---
[x] create forge-core as a SEPARATE cargo workspace; freeze the .fci schema (ChunkRecord
    mirroring builder.ChunkSpec, EmbeddingRequest carrying space+model_fingerprint, Entity,
    Edge, BlobRef)
    -> forge-core/ at the repo root with its OWN [workspace] Cargo.toml (NOT a member of the
       sovereign crates/* workspace; cargo metadata confirms the sovereign set stays the four
       crates). minimal deps: serde, serde_json, thiserror; edition 2024; never panic (typed
       ForgeError). schema: ChunkRecord (exact ChunkSpec mirror, verified against python/builder.py),
       EmbeddingRequest{chunk_index, space:SpaceTag, model_fingerprint, payload_ref:PayloadRef},
       Entity+MentionSpan, Edge, BlobRef, and the FciBundle container + FCI_SCHEMA_VERSION (versioned
       independently of NEST_FORMAT_VERSION). deterministic serialize (compact json, declaration
       order, verbatim strings so chunk_id never desyncs) + validate (cross-reference + version,
       fail-closed). tests: byte-identical roundtrip, two-build determinism, validate rejections.
[ ] deterministic normalize + native-rust pdf-text + plaintext + books.toml (DEFERRED to forge-0b/#09)
[ ] python/forge adapter + forge ingest/profiles/plan cli (DEFERRED to forge-0b/#09)
    note: per the owner, this #05 is FORGE-0a = the forge-core workspace + the .fci schema
    FREEZE (the foundation #04 and real ingestion consume). normalize/, extract/pdf_text.rs,
    profile/, python/forge/adapter.py and cli.py are owned by forge-0b (#09) too, and the
    adapter needs an embedder (#04's static model2vec) and native pdf deps, so they ride #09.
    the chunk_id-parity golden test (forge-built == builder.Pipeline-built) lands with the
    adapter in #09; forge-core deliberately does NOT chunk (one chunker = builder.chunk_text).

--- draws from ---
run-llama/semtools, activeloopai/deeplake, jvm-repo-rebuild/reproducible-central, openai/CLIP

--- watch out ---
  two chunkers WILL drift; keep ONE (builder.chunk_text) and have forge call it rather than
  duplicate-then-reconcile, which is the more expensive path for a solo author.

--- NEST RULES (check each) ---
[ ] edition 2024; never panic in lib (typed thiserror). FORGE heavy/non-deterministic work
    stays OUT of the sovereign crates/ workspace.
[ ] format v1 FROZEN + ADDITIVE: no NEST_FORMAT_VERSION bump. use reserved encoding ids 4-9 /
    section ids 0x09+.
[ ] content_hash over DECODED bytes: codecs decode byte-identically so nest:// citations stay
    stable.
[ ] never zstd/dedup/shuffle the embeddings section (mmap SIMD hot path); only fixed-stride
    quantization there.
[ ] deterministic: the .nest build anchor is canonical extracted text + model fingerprint,
    verify byte-identical across two builds.
[ ] <=300 lines per rust src file; docs lowercase, no emoji, no em-dash. feature branch off
    main.

--- DONE WHEN (all green) ---  (FORGE-0a)
[x] sovereign gate untouched: ./scripts/release_check.sh ALL GATES PASS, 151 rust tests, golden
    byte-identical (forge-0a touches ZERO core files; cargo metadata confirms forge-core is not
    in the sovereign workspace)
[x] forge-core own gate: cargo build/test (6 tests) + fmt --check + clippy -D warnings, all green,
    every file <=300 lines, via --manifest-path forge-core/Cargo.toml
[x] measure_presets.py: size ratio + recall@10 unchanged (forge-0a adds no .nest bytes)

--- RESULT (fill in) ---
  size ratio  before: exact 1.000 / compressed 0.350 / tiny 0.283 / hybrid 0.668
              after:  exact 1.000 / compressed 0.350 / tiny 0.283 / hybrid 0.668  (unchanged)
  recall@10   before: 1.0000 / 1.0000 / 0.9920 / 1.0000
              after:  1.0000 / 1.0000 / 0.9920 / 1.0000  (unchanged; forge-0a touches zero core)
  forge-core: new separate workspace, 6 tests green, byte-identical .fci roundtrip + determinism.

--- DOC CHECKPOINT (virtuous cycle: keep the living docs in sync, as a team) ---
update only what THIS task actually changed; check each:
[ ] doc/arc/arc.md     architecture inventory + contract (boundaries/sections/data-flow/runtime)
[ ] doc/arc/arc.yaml   machine architecture map (MUST move with arc.md, same commit)
[ ] doc/arc/arc.mmd    mermaid build/query flow (if the flow changed)
[ ] README.md          overview / cli / presets / highlights (if a user-visible surface changed)
[ ] doc/usage.md       the how-to (if a command, flag, preset, or workflow changed)
[ ] AGENTS.md          build/test cmds, conventions, gotchas, known gaps (if any changed)
[ ] Cargo.toml         workspace version / deps / new crate (if crates or deps changed)
[ ] pyproject.toml     python tooling / deps / targets (if the python surface changed)
[ ] rustfmt.toml       only if a formatting rule must change (rare)
the arc trio (arc.md + arc.yaml + arc.mmd) moves TOGETHER in the same commit when
architecture / boundaries / data-flow / contracts / storage / runtime change.

--- MERGE ---
[ ] commit on branch feat/p0-05-forge-0-fci-freeze-books-profile-parallel-touc
[ ] gate green -> merge, mark done in plan/kanban.txt
