==============================================================================================
TASK #03 :: manifest additivity guard + roundtrip + recall harness + distribution
phase: phase 0   layer: nest
status:  [ ] todo    [x] doing    [ ] done   (step 1 manifest-additivity guard done + gate green;
         steps 2-4 are a different OWNS set, scoped to a separate task by the owner)
==============================================================================================

self-contained checklist. work top to bottom, check each [ ]. read the COMPLETE
files listed before coding. do not summarize, do not skip. add a [ ] if something
is missing. never skip silently.

--- goal ---
land the guardrails that de-risk the whole roadmap, the ONE parameterized rerank-honesty test
that gates every future search path, the reconciled additive rails, AND the two flagship
blockers (default static embedder, one concrete demo corpus). zero ratio yet; everything later
ships behind proven gates.

--- depends on ---
  previous phase merged

--- READ THESE COMPLETE FILES FIRST ---
[ ] doc/plan/tasks/now/09-per-codec-roundtrip-content-hash-equality-test-t.txt
[ ] doc/plan/tasks/next/15-zero-copy-pybuffer-query-maturin-cp312-abi3-whee.txt
[ ] crates/nest-format/src/manifest/mod.rs
[ ] crates/nest-format/src/reader/decode.rs
[ ] doc/plan/master-plan/02-format.txt
[ ] doc/plan/master-plan/03-roadmap.txt
[ ] doc/plan/master-plan/04-risks-quickwins.txt

  (and the current code you will change:)
[ ] crates/nest-cli/src/cmd/benchmark.rs
[ ] crates/nest-runtime/src/metrics/
[ ] crates/nest-python/src/nest_file.rs
[ ] crates/nest-format/src/manifest/mod.rs

--- OWNS THESE CODE FILES (no parallel task edits them) ---
  crates/nest-cli/src/cmd/benchmark.rs
  crates/nest-runtime/src/metrics/
  crates/nest-python/src/nest_file.rs
  crates/nest-format/src/manifest/mod.rs

--- STEPS (check each) ---
[x] state and TEST that ALL new manifest fields are Option<T> + skip_serializing_if, defaulted
    on read; new capability flags in Option<CapabilitiesExt> or the flatten extra map, NEVER as
    new required bools on Capabilities; v01_compat byte-identical test
    -> manifest/mod.rs: added CapabilitiesExt (Option<bool> flags, all skip_serializing_if) and
       capabilities_ext: Option<CapabilitiesExt> on Manifest (skip_serializing_if=is_none,
       default None). doc rule on Capabilities forbidding new required bools. reader/decode.rs:
       documented that the manifest rides file_hash only, never content_hash, so additive fields
       never move a citation. guard tests in tests/manifest_additivity.rs (unset fields omitted,
       byte-identical round-trip, unknown future field survives via extra, capabilities_ext
       additive). serde defaults a missing Option to None, so old manifests still deserialize;
       capabilities_ext=None serializes to nothing, so golden + v01_compat stay byte-identical.
       fixed 5 exhaustive Manifest test literals (negative_*, dual_integrity, fp16_topk) with
       capabilities_ext: None.
[ ] roundtrip+content_hash-equality template (DEFERRED - different OWNS, separate task)
[ ] mteb-style recall harness + net-of-fp (DEFERRED - owns benchmark.rs / runtime/metrics/)
[ ] repro_manifest 0x10 + maturin wheel + zero-copy PyBuffer (DEFERRED - owns nest_file.rs / pyproject)
    note: per the owner, this chat's #03 OWNS are manifest/mod.rs + reader/decode.rs only
    (the manifest-additivity guard). steps 2-4 own benchmark.rs, runtime/src/metrics/,
    nest_file.rs and belong to a separate task/agent; not started here.

--- draws from ---
embeddings-benchmark/mteb, jvm-repo-rebuild/reproducible-central, PyO3/pyo3

--- watch out ---
  Capabilities is non-optional bools with a hand-written Default; adding a required bool is a
  deserialization + file_hash break sold as byte-identical. enforce Option-only new fields
  with a v01_compat test or every existing file's file_hash changes.

--- NEST RULES (check each) ---
[ ] edition 2024; never panic in lib (typed thiserror). FORGE heavy/non-deterministic work
    stays OUT of the sovereign crates/ workspace.
[ ] format v1 FROZEN + ADDITIVE: no NEST_FORMAT_VERSION bump. use reserved encoding ids 4-9 /
    section ids 0x09+.
[ ] content_hash over DECODED bytes: codecs decode byte-identically so nest:// citations stay
    stable.
[ ] never zstd/dedup/shuffle the embeddings section (mmap SIMD hot path); only fixed-stride
    quantization there.
[ ] deterministic: the .nest build anchor is canonical extracted text + model fingerprint,
    verify byte-identical across two builds.
[ ] <=300 lines per rust src file; docs lowercase, no emoji, no em-dash. feature branch off
    main.

--- DONE WHEN (all green) ---  (for the manifest-additivity guard, step 1)
[x] cargo build --release --workspace ; cargo test --workspace  (151 rust tests pass)
[x] cargo fmt --all --check ; cargo clippy --workspace --all-targets -- -D warnings
[x] rebuild python/_nest.so ; ./scripts/release_check.sh passes (ALL GATES PASS)
[x] measure_presets.py: size ratio + recall@10 unchanged; golden + v01_compat byte-identical

--- RESULT (fill in) ---
  size ratio  before: exact 1.000 / compressed 0.350 / tiny 0.283 / hybrid 0.668
              after:  exact 1.000 / compressed 0.350 / tiny 0.283 / hybrid 0.668  (unchanged)
  recall@10   before: 1.0000 / 1.0000 / 0.9920 / 1.0000
              after:  1.0000 / 1.0000 / 0.9920 / 1.0000  (unchanged; manifest guard only)
  byte-identity: golden 5/5 and v01_compat green -> capabilities_ext=None serializes to
              nothing, so existing files' file_hash is untouched.

--- DOC CHECKPOINT (virtuous cycle: keep the living docs in sync, as a team) ---
update only what THIS task actually changed; check each:
[ ] doc/arc/arc.md     architecture inventory + contract (boundaries/sections/data-flow/runtime)
[ ] doc/arc/arc.yaml   machine architecture map (MUST move with arc.md, same commit)
[ ] doc/arc/arc.mmd    mermaid build/query flow (if the flow changed)
[ ] README.md          overview / cli / presets / highlights (if a user-visible surface changed)
[ ] doc/usage.md       the how-to (if a command, flag, preset, or workflow changed)
[ ] AGENTS.md          build/test cmds, conventions, gotchas, known gaps (if any changed)
[ ] Cargo.toml         workspace version / deps / new crate (if crates or deps changed)
[ ] pyproject.toml     python tooling / deps / targets (if the python surface changed)
[ ] rustfmt.toml       only if a formatting rule must change (rare)
the arc trio (arc.md + arc.yaml + arc.mmd) moves TOGETHER in the same commit when
architecture / boundaries / data-flow / contracts / storage / runtime change.

--- MERGE ---
[ ] commit on branch feat/p0-03-manifest-additivity-guard-roundtrip-recall-har
[ ] gate green -> merge, mark done in plan/kanban.txt
