==============================================================================================
nest build board v2
now / next / gate / later / research
==============================================================================================

tagged by layer (forge/nest/lens/cross)

##############################################################################################
# NOW  (9 cards)
##############################################################################################

[x] land SECTION_EMBEDDINGS_FP=0x09 + refactor score_subset to a rerank-source handle
    (CRITICAL/nest)  [p0 #01: impl + gate green; 0x09 writer deferred to p3 #14]
    the gate for ALL sub-int8 AND all per-space sub-int8 work. score_subset today
    hard-dispatches on a single self.dtype and the single 0x04 section (search.rs:84-121);
    refactor it to take an explicit rerank-source handle (fp slab when present) AND be ready
    for per-space routing BEFORE any per-space/sub-int8/graph path lands. additive, no version
    bump.
    refs: crates/nest-runtime/src/search.rs, crates/nest-format/src/sections/contract.rs,
          gaoj0017/RaBitQ

[x] ONE parameterized rerank-honesty contract test (gates the release check)   (CRITICAL/nest)
    [p0 #01: tests/rerank_contract.rs; covers ann+hybrid, new verbs must join the list]
    single test over every search entry point (ann/hybrid/graph/space/cross) asserting
    returned_score == score_subset(union) byte-for-byte and recall is NaN for every non-exact
    path. a new search verb without this test is a release-check FAILURE. the cheapest
    insurance for the core honesty claim; must exist BEFORE any new search path.
    refs: crates/nest-runtime/src/search.rs, crates/nest-cli/src/cmd/benchmark.rs,
          gaoj0017/RaBitQ

[x] fix materialize-to-f32 ram leak in the ann distance path   (CRITICAL/nest)
    [p0 #01: materialize.rs -> PackedVectors, packed int8/f16 rows decoded per-row]
    materialize_f32_vectors expands every dtype to n*dim*4 f32 when hnsw is attached. teach
    the ann path to score packed int8 blocks. pure runtime fix; carried per-space when
    multimodal lands.
    refs: crates/nest-runtime/src/materialize.rs, crates/nest-runtime/src/ann/mod.rs,
          microsoft/onnxruntime

[x] flagship blocker: offline static default embedder (FLOOR only)   (CRITICAL/forge)
    [p0 #04: python/forge/embed_default.py, offline stdlib-only static embedder, deterministic,
     f32-stable, self-fingerprinting model_hash; brought-model path kept as quality ceiling]
    the offline-by-construction blocker is CLOSED. but the shipped default is a randomized
    bag-of-words (token -> fixed pseudo-random vector, text -> normalized mean): cosine reflects
    shared LITERAL tokens only, NO semantic generalization (carro vs automovel ~ carro vs
    banana). good enough as an offline FLOOR; the real model2vec/potion table is a separate
    REQUIRED card below.
    refs: python/forge/embed_default.py, crates/nest-cli/src/main.rs, minishlab/model2vec

[ ] vendor a REAL model2vec/potion static table (semantic, drop-in)   (HIGH/forge)
    [p1 #10: DELIVERED on branch research/nest-genome-v2, awaiting reviewer eyes + maintainer
     merge (NOT merged, NOT marked done). vendored minishlab/potion-base-8M (mit, dim 256, table
     29528x256 f32, ~30mb git-lfs) behind the same StaticEmbedder interface + sha256 model_hash;
     numpy+tokenizers offline inference (no torch, no network, matches model2vec vector-for-vector).
     semantic jump car~automobile +0.78 vs car~banana +0.04 (floor separates -0.09); recall@1 on
     demo_corpus potion 0.875 vs floor 0.250. embed_potion.py + test_embed_potion.py + recall_harness.py]
    the #04 default is lexical-only; the flagship pitch is SEMANTIC retrieval. vendor an actual
    distilled model2vec/potion table (still offline, tiny, no torch) behind the SAME
    StaticEmbedder interface + model_hash convention, so it is a drop-in swap. REQUIRED before
    the flagship demo (#08) and the gate, or the one-gif demo retrieves by keyword and the
    'semantic' claim is hollow. validate per-query recall vs the lexical floor.
    refs: python/forge/embed_default.py, minishlab/model2vec, run-llama/semtools

[x] flagship blocker: pick ONE concrete license-clean demo corpus now   (HIGH/forge)
    [p0 #04: python/forge/demo_corpus/, cc0 original docs about nest itself, redistributable]
    every vertical demo is currently 'a synthetic monolith' with no actual corpus chosen. the
    one-gif flagship demo needs a concrete, vendored, permissively-licensed doc folder picked
    NOW; it gates the whole go-to-market. cheap and currently absent. never academic-only
    data.
    refs: python/forge/demo_corpus/, doc/plan/master-plan/00-vision.txt, run-llama/semtools

[x] codec registry refactor + reserve ALL reconciled ids in one pass (incl 0x16 overlay)
    (HIGH/nest)  [p0 #02: WireCodec registry + encode_smallest + 0x11-0x16/0x20-0x3F reserved]
    visitor-style registry; reserve encoding 4-9 and section 0x09-0x10 (present) AND pre-claim
    0x11 GRAPH_NODES / 0x12 GRAPH_EDGE_PROPS / 0x13 GRAPH_ENTITY_MAP / 0x14 BLOB_REFS / 0x15
    SPACE_TABLE / 0x16 BLOB_SPAN_OVERLAY / 0x20-0x2F slabs / 0x30-0x3F fp, ALL excluded from
    content_hash, disjoint-bands test. 0x16 REPLACES the illegal spans v1->2 bump. removes the
    four-pillar 0x11 collision before any feature edits layout.
    refs: crates/nest-format/src/encoding/mod.rs, crates/nest-format/src/layout/mod.rs,
          serde-rs/serde

[x] manifest additivity guard: new fields Option-only + v01_compat test   (HIGH/nest)
    [p0 #03 step1: CapabilitiesExt + capabilities_ext Option, tests/manifest_additivity.rs,
     byte-identical (golden+v01_compat). steps 2-4 (roundtrip template, recall harness,
     maturin/PyBuffer) deferred to a separate task/OWNS]
    Capabilities (manifest/mod.rs:21-40) is non-optional bools with a hand-written Default;
    adding a required bool is a deserialization + file_hash break sold as byte-identical.
    enforce: ALL new manifest fields Option<T> skip_serializing_if=is_none, new capability
    flags in a new Option<CapabilitiesExt> or the flatten extra map, never new required bools.
    test: old manifest deserializes, new default serializes byte-identical.
    refs: crates/nest-format/src/manifest/mod.rs, crates/nest-format/src/reader/decode.rs,
          serde-rs/serde

[ ] per-codec roundtrip+content_hash template + mteb-style recall harness (net-of-fp)
    (HIGH/nest)
    every new codec AND every new space/graph/blob/overlay section must pass decode==input and
    content_hash-stable; seeded recall@k vs exact f32 per preset + byte-identical-build ci
    check; every sub-int8 RESULT reports the NET-OF-FP ratio (quantized + mandatory fp
    source), never the bare quantized number.
    refs: crates/nest-cli/src/cmd/benchmark.rs, crates/nest-format/src/encoding/mod.rs,
          embeddings-benchmark/mteb

[x] forge-0a: freeze .fci schema + forge-core skeleton (ONE chunker, zero core files)
    (HIGH/forge)  [p0 #05: forge-core separate workspace + frozen .fci (FciBundle/ChunkRecord/
     EmbeddingRequest/Entity/Edge/BlobRef) + deterministic serialize; sovereign gate untouched.
     normalize/pdf/adapter/cli ride forge-0b/#09]
    forge-core as a SEPARATE cargo workspace (deps never touch the sovereign core).
    ChunkRecord mirrors builder.ChunkSpec; EmbeddingRequest carries space+model_fingerprint;
    Entity/Edge/BlobRef. do NOT duplicate the chunker: the python/forge adapter CALLS
    builder.chunk_text so there is one authoritative chunker and no drift to reconcile.
    unblocks real corpora, touches zero rust-core files.
    refs: forge-core/src/fci/, python/builder.py, doc/research/synthesis/data-pipeline.txt


##############################################################################################
# NEXT  (9 cards)
##############################################################################################

[x] shared intpack codec (delta+zigzag+per-128-block bitpack/FOR, O(1) select)   (HIGH/nest)
    [p1 #06: encoding/intpack.rs, per-128-block FOR, IntpackReader::get O(1), typed errors,
     never panics on truncated/hostile input (tamper test); implements reserved id 4]
    SECTION_ENCODING_INTPACK=4, the reusable primitive for hnsw/bm25/spans AND graph csr AND
    space row_index_map AND blob_refs offsets. branch-free decode, never panic on truncated
    input -> typed thiserror.
    refs: crates/nest-format/src/encoding/intpack.rs, lemire/FastPFor, cruppstahl/libfor

[x] re-encode hnsw neighbor ids + bm25 postings with intpack   (HIGH/nest)
    [p1 #06: ann/codec.rs + bm25/codec.rs v2 payloads (readers accept v1+v2), order-preserving
     FOR-bitpack (NOT sort+delta, to keep ef-pruning candidate set -> recall identical), hnsw v2
     decode hardened vs hostile level/degree (adversarial review). tiny hnsw 3.61->1.69MB]
    sort-per-list + delta+bitpack hnsw (flat uint40, bump the OPTIONAL hnsw payload version,
    zero recall change); delta-gap bm25 doc ids + bitpacked tf. biggest free shrink of the
    tiny/hybrid index sections. spans stays canonical and is NEVER version-bumped.
    refs: crates/nest-runtime/src/ann/codec.rs, crates/nest-runtime/src/bm25/codec.rs,
          apache/lucene

[x] chunk_ids as 32 raw sha-256 bytes + spans source_uri dedup + offset tables   (HIGH/nest)
    [p1 #06: sections/chunk_ids.rs + spans.rs intpack repacks; chunk_ids 32 raw digest bytes
     (falls back to raw on any non-canonical id, guards uppercase hex), spans deduped uri pool
     (O(1), 5efef331) + bitpacked offsets, pick-smaller vs zstd. decode byte-identical ->
     content_hash stable. tiny chunk_ids 2.30->0.98MB. offset table deferred (not needed yet)]
    32 raw digest bytes not 71-byte ascii (~2.2x, content_hash unchanged); dedup source_uri
    pool + delta+varint span offsets; u32 offset table for O(1) chunk-N reach off mmap.
    decoded content_hash bytes stay identical so citations are stable.
    refs: crates/nest-format/src/sections/chunk_ids.rs,
          crates/nest-format/src/sections/spans.rs, google/flatbuffers

[ ] G1: graph_adjacency(0x0C) chunk csr + search-graph + drop chunk_overlap   (HIGH/cross)
    flat csr reusing intpack, bounded-bfs seeded from exact top-k feeding the refactored
    score_subset handle (recall NaN, score real cosine, covered by the phase-0 parameterized
    test), kept under the hood as the 9th subcommand. reclaims chunk_overlap via NEXT_CHUNK.
    EARNS its phase-1 place on compression+retrieval merit; DECOUPLED from the entity graph -
    landing G1 is not license to start G2/G3.
    refs: crates/nest-format/src/sections/graph/adjacency.rs,
          crates/nest-runtime/src/graph/csr.rs, graph-db/vesoft-inc__nebula

[ ] flagship: ask + cite (tier-1) + retrieve() + SearchExplain   (HIGH/lens)
    the FLAGSHIP. ship TWO user-facing verbs ask + cite; keep the existing 8 subcommands under
    the hood (DEFER verb-collapse + nest-profile crate - churn with no user value pre-users).
    retrieve() returns cited spans where the returned score IS the exact rerank value; cite is
    TIER-1 ONLY (stored canonical text + verifying hashes, exactly cite.rs today) and MUST NOT
    claim original-byte reopen; additive SearchExplain honesty line on ask. needs only phase
    0-1.
    refs: crates/nest-cli/src/cmd/ask.rs, crates/nest-cli/src/cmd/cite.rs,
          python/forge/retrieve.py

[ ] zero-copy PyBuffer query + maturin cp312-abi3 wheel   (MEDIUM/nest)
    PyBuffer<f32> borrow + Python::detach around the read-only mmap search; ship one wheel.
    stops compression wins being paid back as per-search alloc+memcpy and closes the install
    gap for the flagship.
    refs: crates/nest-python/src/nest_file.rs, pyproject.toml, PyO3/pyo3

[ ] forge-0b: normalize + books profile + python adapter + drift lock   (HIGH/forge)
    deterministic nfc/whitespace normalize; native-rust pdf-text + books.toml; python/forge
    adapter driving the EXISTING builder.Pipeline and calling builder.chunk_text (one
    chunker). ci golden test: forge-built == pipeline-built for the same canonical text (locks
    the single chunker).
    refs: forge-core/src/normalize/, forge-core/src/extract/pdf_text.rs,
          python/forge/adapter.py

[ ] trained shared zstd dictionary + fsst + zstd level/ldm knobs (PRE-GATE, rebalanced)   (HIGH/nest)
    dictionary(0x0A) + zstd_dict(5) over sorted samples (deterministic, pinned zstd version) +
    fsst(9) for O(1) single-chunk reopen; replace hardcoded level-19. headline text lever;
    also serves forge's heterogeneous text inputs unchanged.
    refs: crates/nest-format/src/encoding/zstd_dict.rs,
          crates/nest-format/src/encoding/fsst.rs, facebook/zstd
    NOTE (WO TXT-dict-fsst-dedup, 2026-06-06, branch research/nest-genome-v2): the three SAFE
    text levers SHIPPED, all additive within frozen v1, all decoding chunks_canonical
    BYTE-IDENTICALLY (content_hash + citations unchanged) and all chosen per-build by the
    existing try-smaller chooser so they never regress: zstd_dict(5) = TXT_STREAMS_V2 against a
    trained shared dict (deterministic ZDICT from_continuous over SORTED unique texts, pinned
    zstd, dict blob in new SECTION_DICTIONARY 0x0A, content_hash-excluded); fsst(9) =
    TXT_STREAMS_V3 clean-room 255-entry static symbol table embedded in the payload; and
    dedup-before-zstd (new SECTION_DEDUP_MAP 0x0B, content_hash-excluded) on DECOMPRESSED bytes
    (nix/ipfs order rule) + u32 back-refs. honest text-section measurement on the shipped
    all-unique pt-br corpus (n=30725): single-frame zstd-19 = 15.73MB still WINS (dict 21.18MB,
    txt_streams 29.25MB, fsst 48.47MB), so the chooser keeps single-frame zstd, the tiny preset
    stays byte-identical (0.256 ratio, recall@10 0.9900 held), and the file never regresses. the
    dict recovers most of the txt_streams cross-chunk-context loss (+85.9% -> +34.7%) but cannot
    beat one big zstd-19 frame on contiguous all-unique prose; dedup is proven to win only on
    far-apart repeats beyond a single zstd window. STILL TODO on this card: the zstd level/ldm
    knobs and the rotational-int8 micro preset (separate scope).

[ ] block-scaled + rotational int8 (scale_kind=1, no new id, no version bump) (PRE-GATE, rebalanced)
    (HIGH/nest)
    one f16 scale per 32-dim block + seeded fwht rotation toward ~0.99 recall@10 at 4x; the
    int8 scale_kind field already exists. compute rotation backend-independently for cross-cpu
    determinism. ships the micro preset below tiny 0.283 - the number the gate publishes.
    refs: crates/nest-format/src/encoding/int8.rs, crates/nest-format/src/encoding/fwht.rs,
          ggml-org/ggml


##############################################################################################
# GATE  (1 cards)
##############################################################################################

[ ] FLAGSHIP-PROVEN gate (blocks all of later/research)   (CRITICAL/cross)
    NOTHING in later/research gets code until BOTH: (1) the micro preset (dict + fsst +
    block/rotational int8, landed pre-gate) measured well below tiny 0.283, proven and published
    on ONE real, named, license-clean corpus, recall@10 held (sub-0.1 is the post-gate stretch
    with int4/rabitq, net-of-fp);
    (2) the agent-native pip package has external users. converts the discipline from 'build
    everything additively' into 'build the wedge, prove it, earn the rest'. the single most
    important artifact missing from the draft.
    refs: doc/plan/master-plan/gate.txt, embeddings-benchmark/mteb,
          jvm-repo-rebuild/reproducible-central


##############################################################################################
# LATER  (7 cards)
##############################################################################################

[ ] original-byte reopen path + catalog blob_refs(0x14) + 0x16 overlay + .nestpack (post-gate)
    (MEDIUM/nest)
    BUILD tier-2 original-byte reopen FIRST (cite today returns stored canonical text only,
    never opens source_uri - cite.rs:43-62), then blob_refs. catalog blob-relative spans go in
    the SEPARATE excluded 0x16 overlay keyed by chunk ordinal, NOT a spans v1->2 bump (spans
    is canonical+content-hashed+required; a v2 there breaks every reader and changes
    content_hash). cross-mode content_hash-equality test; .nestpack sidecar (magic NPAK)
    strictly optional; 'verifiable, original unavailable' is a first-class typed state.
    refs: crates/nest-cli/src/cmd/cite.rs, crates/nest-format/src/sections/blob_refs.rs,
          content-addressed/ipfs__kubo

[ ] int4 block-64 + rabitq 1-bit + dedup-before-zstd + chunk_scalars (post-gate)
    (MEDIUM/nest)
    int4(7) fused dequant+dot + rabitq(8) popcount candidate gen, both gated on the fp rerank
    source via the score_subset handle -> real cosine; dedup(0x0B) on decompressed bytes
    before zstd + chunk_scalars(0x0D) pre-filter. RESULT reports NET-OF-FP ratio (rabitq + f16
    fp ~ roughly 2x, never bare 32x).
    refs: crates/nest-format/src/encoding/int4.rs, crates/nest-format/src/encoding/rabitq.rs,
          NixOS/nix
    note (EMB-int4): the int4(7) leg is DONE. block-64 per-group f16 absmax scales + packed
          4-bit codes; fused dequant+dot kernel (avx2/neon/scalar, bit-for-bit); stored-precision
          rerank like int8 (no fp source), disclosed via dtype; 'nano' preset (zstd/int4/hnsw).
          measured: embeddings 11.92MB -> 6.27MB (1.9x over int8, 7.5x over f32), file
          size_ratio 0.209, recall@10 0.913 vs the f32 exact baseline (100 queries, dim 384).
          named 'nano' (not 'micro') to keep 'micro' reserved for the dict+fsst+int8 task.
          still open on #14: rabitq(8), dedup-before-zstd(0x0B), chunk_scalars(0x0D).

[ ] forge-1: belt subprocess contract + paddleocr/clip/fonttools/stanza + osint/fonts profiles
    (post-gate)   (MEDIUM/forge)
    opt-in --belt heavy engines behind a json-over-stdio contract so deps never enter the rust
    tree; engine_version recorded in provenance; each non-text space carries its own
    model_fingerprint; cache keyed by (input_hash, engine_version). fail closed on missing
    engine, never network-fetch.
    refs: forge-core/src/belt/, python/forge/belt_engines/, openai/CLIP

[ ] G2/G3: entity property graph + graphrag traversal + export api (post-gate)
    (MEDIUM/cross)
    graph_nodes(0x11)+graph_edge_props(0x12)+graph_entity_map(0x13), excluded from
    content_hash; entity-aware bounded-bfs + integer edge-type filter feeding the score_subset
    handle; versioned export api (node->citation). NO query language, NO planner, NO write
    path. this is the second product the draft front-loaded; only after the flagship is
    proven.
    refs: crates/nest-format/src/sections/graph/nodes.rs,
          crates/nest-runtime/src/graph/export.rs, semantic-rdf/terminusdb__terminusdb-store

[ ] osint-lake vertical + cosmos.gl/webgl viewer (post-gate)   (MEDIUM/cross)
    standalone lens/ web project reading the export json, accessibility-first (seeded layout,
    type-as-color, keyboard, citation-on-click), no socket, svg/png fallback, vendored deps, a
    check asserts no remote fetch(); lake build/search demo over a synthetic or permissive
    monolith.
    refs: lens/src/graph_view.ts, python/tools/demo_lake.py,
          graph-db/neo4j__neo4j-graphrag-python

[ ] matryoshka prefix truncation + micro/nano preset ladder (post-gate)   (MEDIUM/nest)
    build-time truncate+L2-renorm to mrl_dim, two-pass coarse->exact, multiplicative with
    quant; micro/nano presets below tiny's 0.283 with measured recall, ratio net-of-fp where
    sub-int8.
    refs: crates/nest-python/src/build_fn.rs, QwenLM/Qwen3-Embedding

[ ] byte-level subword tokenizer + append-only edit journal (post-gate)   (LOW/nest)
    tokenizer_model(0x0E) byte-level subword closing CJK/Thai bm25 + doubling as a text
    front-end; edit_journal(0x0F) overlaid in memory at open + deterministic compact(). vocab
    frozen+fingerprinted.
    refs: crates/nest-runtime/src/bm25/tokenize.rs, crates/nest-runtime/src/journal.rs,
          google/sentencepiece


##############################################################################################
# RESEARCH  (7 cards)
##############################################################################################

[ ] multimodal: SPACE_TABLE(0x15) + per-space route + per-space quant + atlas verb (post-gate)
    (MEDIUM/nest)
    the one genuinely new format capability and the pattern-lover prerequisite. named space
    sections over 0x15/0x20-0x2F/0x30-0x3F, per-space route feeding the ALREADY-refactored
    score_subset handle (phase 0) and already covered by the parameterized honesty test,
    per-space quant gated by per-space fp source, RESULT net-of-fp. open question: named
    sub-sections vs single tagged pool - prototype both on the .fci.
    refs: crates/nest-format/src/sections/spaces/, crates/nest-runtime/src/spaces/,
          vector-db/qdrant__qdrant

[ ] pattern-lover instrument vertical (voynich/gematria/unicode/etymology) (post-gate)
    (LOW/lens)
    the soul; ships LAST on the proven engine. decide whether symbol/glyph search needs a
    bespoke symbol embedder or can ride the clip space treating codepoints as images; this
    decides one new model vs several. permissively-licensed corpus only.
    refs: python/forge/profiles/pattern.toml, python/tools/demo_pattern.py,
          embeddings-models/openai__CLIP

[ ] catalog-3: sub-file range references + blob dedup + parquet/warc wiring (post-gate)
    (LOW/forge)
    reference parquet row groups / warc records / zip members by whole-blob content-hash +
    advisory range; dedup identical references; ALL format parsing stays in python forge, the
    core only stores+reopens byte ranges by hash. the duckdb/parquet-monolith antidote doc.
    refs: crates/nest-format/src/sections/blob_refs_dedup.rs, python/forge/catalog.py,
          content-addressed/NixOS__nix

[ ] BLAKE3 Merkle-root content_hash (the one true v2)   (LOW/nest)
    the ONLY justified format bump, deferred. changes the meaning of the citation anchor so it
    cannot be additive. ship additive merkle_index + a blake3 algorithm-id for large-blob
    addressing FIRST; bump only on measured demand for partial/single-chunk verification.
    nothing in the redesign forces it.
    refs: crates/nest-format/src/hashing.rs, crates/nest-format/src/sections/merkle_index.rs,
          BLAKE3-team/BLAKE3

[ ] structured tape (json/yaml/parquet) + per-column codec cascade (post-gate)   (LOW/nest)
    fixed-stride u64 tape + length-prefixed value blob (simdjson-style), sub-document citable,
    per-column duckdb-style analyze-and-pick. portable parity scan for cross-cpu determinism.
    the compress-and-serve-structured-data lever for ml dataset catalogs.
    refs: crates/nest-format/src/encoding/structure.rs, simdjson/simdjson, duckdb/duckdb

[ ] optional redaction + provenance facet + at-rest encryption (ethics, never gates)
    (LOW/cross)
    build-time redaction pass (content_hash over redacted bytes, no new section, distinct
    redacted corpus), post-rerank provenance facet (score stays real cosine), seal/unseal
    outer encryption OUTSIDE the frozen container. every feature acts only on the user's own
    data at their command; no policing, no telemetry.
    refs: python/forge/redaction.py, crates/nest-cli/src/cmd/seal.rs,
          content-addressed/NixOS__nix

[ ] sovereign in-house fse/tans entropy floor + algorithm-4 hnsw selection (post-gate)
    (LOW/nest)
    fse/tans entropy floor over delta-coded postings/adjacency below generic zstd;
    malkov-yashunin algorithm-4 neighbor selection so tiny/nano presets lower M and shrink the
    index. larger surface, lowest priority.
    refs: crates/nest-format/src/encoding/fse.rs,
          crates/nest-runtime/src/ann/select_neighbors.rs, Cyan4973/FiniteStateEntropy
