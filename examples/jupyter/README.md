# jupyter + nestdb example

`nestdb_minimal.ipynb` builds a tiny potion-embedded corpus, validates it,
and runs one cited retrieve, all offline. setup:

```
pip install "nestdb[embed]" jupyter
jupyter notebook nestdb_minimal.ipynb
```
